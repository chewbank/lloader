'use strict';

module.exports = {
   sub: {
      level: 2,
      // module(data) {
      //    console.log(`controller module ${data}`)
      // },
      // directory(data) {
      //    console.log('controller directory')
      // }
   },
   'db.js': {
      level: 1
   }
}